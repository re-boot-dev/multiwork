class Header extends HTMLElement {
  constructor() {
    super();
  }
}

/* i want to create a header element so it can be replicated across all pages, but ill brush up more on my js first. -alex */ 

